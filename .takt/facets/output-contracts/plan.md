```markdown
# タスク計画

<!--
書式規約（この案件専用。理由: takt は次ステップの命令にレポート本文をそのまま埋め込み、
Linux の 1 引数上限 128KB を超えると execve が E2BIG で失敗する）

- **このレポート全体を 12KB 以内に収める。** 超えそうなら列の説明文を削る。行は削らない。
- 根拠は必ず `file:line` のポインタで示す。原文を引用しない。読み手はファイルを開ける。
- 要求の逐語コピーを載せない。`order.md` への参照で足りる。
- 方針文書（CONTEXT.md, docs/adr/*, docs/diagnostics.md）の内容を書き写さない。参照だけ書く。
- 同じ内容を 2 か所に書かない。「同上」も書かない。行を統合する。
- セルは名詞句で書く。文末表現、接続詞、丁寧語を入れない。
- 該当しない節は見出しごと省く。「なし」とだけ書かれた節を残さない。
-->

## 目的
{1〜2 文}

## 要件
| # | 要件 | 要否 | 由来 |
|---|------|------|------|
| 1 | {検証可能な最小単位} | 要 / 不要 `file:line` | `order.md:N` / 導出 / 既存契約 `file:line` |

- 並列表現（`A/B`、`allow/deny` 等）は別行にする
- 設計判断は要件にしない。「検討したアプローチ」へ置く

## 検討したアプローチ（設計判断がある場合のみ）
| 案 | 採否 | 理由 |
|----|------|------|

## 完了契約
| ID | 成立する振る舞い | 拒否する誤実装 | 実装箇所 | 完了証拠 |
|----|------------------|----------------|----------|----------|
| `C1` | {観測可能な成功条件} | {もっともらしい反例} | `file` | {観測方法} |

## 影響経路（該当する契約のみ）
| ID | 生成元 | 消費元・入口 | 状態・所有権 | 移行対象 |
|----|--------|--------------|--------------|----------|

「状態・所有権」は、要求が特定の変化を名指しし、その変化をまたいで同じ実体が存続する場合のみ記入する。

## 到達経路（利用者向け機能の追加・変更がある場合のみ）
| 入口 | 配線の更新対象 | 起動条件 | 未対応 |
|------|----------------|----------|--------|

## スコープ外 / 確認事項（あるもののみ）
| 項目 | 理由 |
|------|------|
```
